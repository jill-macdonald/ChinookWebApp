@page
@model ChinookWebApp.Pages.Albums.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Album</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
@*             As this is auto incremented, is is likely not required on the create page
                <div class="form-group">
                <label asp-for="Album.AlbumId" class="control-label"></label>
                <input asp-for="Album.AlbumId" class="form-control" />
                <span asp-validation-for="Album.AlbumId" class="text-danger"></span>
            </div> *@
            <div class="form-group">
                <label asp-for="Album.Title" class="control-label"></label>
                <input asp-for="Album.Title" class="form-control" />
                <span asp-validation-for="Album.Title" class="text-danger"></span>
            </div>
             <div class="form-group">
                <label asp-for="Album.ArtistId" class="control-label"></label>
                <input asp-for="Album.ArtistId" class="form-control" />
                <span asp-validation-for="Album.ArtistId" class="text-danger"></span>
            </div> 
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}